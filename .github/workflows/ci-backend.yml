name: Backend

on:
  push:
    paths:
      - "server/**"
      - ".github/workflows/ci-backend.yml"
  pull_request:
    paths:
      - "server/**"
      - ".github/workflows/ci-backend.yml"

jobs:
  test-backend:
    runs-on: ubuntu-latest

    services:
      mongo:
        image: mongo:7
        ports:
          - 27018:27017
        options: >-
          --health-cmd "mongosh --quiet --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    defaults:
      run:
        working-directory: server

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: server/package-lock.json

      - name: Install dependencies
        run: npm ci

      # Generate config.test.env safely at runtime
      - name: Create config.test.env for tests
        run: |
          cat > config.test.env << 'EOF'
          ATLAS_URI=mongodb://127.0.0.1:27018
          DB_NAME=shopping-list-test
          JWT_SECRET=ci-test-secret
          NODE_ENV=test
          EOF

      - name: Run backend tests
        run: npm test